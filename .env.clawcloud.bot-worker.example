# ClawCloud account B: bot-worker
# Start command:
#   python polling_worker.py

BOT_TOKEN=your_telegram_bot_token
ADMIN_USER_ID=your_telegram_user_id
KV_REST_API_URL=https://your-upstash-rest-url
KV_REST_API_TOKEN=your-upstash-rest-token

# Required so the bot can build deep links back to the admin web:
WEB_APP_URL=https://your-admin-domain/web

# Do not set WEBHOOK_SECRET for polling_worker.py deployments.
